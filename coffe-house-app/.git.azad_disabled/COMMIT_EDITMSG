feat(pdf): optimize menu pdf generation with pdfmake v0.3.x

- Replace addFonts with addVirtualFileSystem for font registration
- Update async API: getBlob/getBase64/getDataUrl/print/download as Promises
- Add multi-format export support (download, blob, base64, dataUrl, print)
- Add configurable background image via bgImagePath signal
- Add logo caching with Signal and validation (MIME type, size 500KB max)
- Add subcategory titles with decorative lines in PDF layout
- Fix PageSize type: Letter -> LETTER
- Centralize dimensions and styles in PDF_CONFIG constant
- Add pdfError signal for UI error reporting
